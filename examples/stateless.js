import React from "react";

const BasicComponent = ({text}) => (
  <div>
    <h1>Hello World</h1>
    <p>This is a simple example of a stateless component, and show this {text}</p>
  </div>
); 

export default BasicComponent;